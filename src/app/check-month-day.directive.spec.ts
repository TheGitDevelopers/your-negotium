import { CheckMonthDayDirective } from "./check-month-day.directive";

describe("CheckMonthDayDirective", () => {
  it("should create an instance", () => {
    const directive = new CheckMonthDayDirective();
    expect(directive).toBeTruthy();
  });
});
